# Query Optimizer for SFDX

<img src="query plan output.png"/>

Convert
```shell
Usage: sfdx dbx:query:explain 

Query optimizer

Flags:
 -u, --orgname ORGNAME  (required) name of scratch org
 -q, --query QUERY      (required) SOQL

help text for dbx:query:explain

```